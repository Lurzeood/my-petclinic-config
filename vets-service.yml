vets:
  cache:
    ttl: 60
    heap-size: 100

---
spring:
  profiles: default

---
spring:
  profiles: docker
  zipkin:
    baseUrl: http://tracing-server:9411
server:
  port: 8083
spring:
  cloud:
    consul:
      host: localhost
      port: 8500
    discovery:
      serviceName: api-gateway
