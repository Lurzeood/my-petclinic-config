vets:
  cache:
    ttl: 60
    heap-size: 100

---
spring:
  profiles: default

---
spring:
  profiles: docker
  zipkin:
    baseUrl: http://tracing-server:9411
  cloud:
    consul:
      host: localhost
      port: 8500
    discovery:
      serviceName: vets-service
server:
  port: 8083
